{
  "name": "credentials-db-prisma",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "generate": "dotenv-flow -- yarn prisma generate",
    "migrate": "dotenv-flow -- yarn prisma migrate",
    "migrate-reset": "dotenv-flow -- yarn prisma migrate reset",
    "db-seed": "dotenv-flow -- yarn prisma db seed",
    "db-push": "dotenv-flow -- yarn prisma db push",
    "studio": "dotenv-flow -- yarn prisma studio"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^9.1.3",
    "dotenv-flow": "3.2.0",
    "dotenv-flow-cli": "1.0.0",
    "prisma": "^4.0.0",
    "prisma-erd-generator": "^1.0.1"
  }
}
